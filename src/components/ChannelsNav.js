import React from 'react'
import SearchBar from './SearchBar'
import ChannelScroller from './ChannelScroller'

const ChannelsNav = () => {
    return(
        <nav>
            <SearchBar />
            <ChannelScroller />
        </nav>
    )
}

export default ChannelsNav